apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: banking-pvc
  namespace: deployment
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: 2Gi
